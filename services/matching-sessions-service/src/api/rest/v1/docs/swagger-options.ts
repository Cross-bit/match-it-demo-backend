/*import { describe } from "node:test";
import swaggerJsdoc from "swagger-jsdoc"

const options = {
    definition: {
        openapi: '3.0.0',
        info: {
            title: 'Matching session service API',
            description: 'This is the REST API for the match-it session management.',
            version: '1.0.0',
        },
    },
    apis: ['../routes/*.ts'], // Paths to files with JSDoc annotations
};



export const swaggerSpec = swaggerJsdoc(options);*/