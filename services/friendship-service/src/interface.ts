/**
 *
 * Defines set of common interfaces/types we want to be able to access through out the whole app.
 *
 */

export enum UserPrivileges { NORMAL = "NORMAL", ADMIN = "ADMIN", TESTER = "TESTER" };

export enum AuthenticationMethod { DEVICE = "DEVICE", CREDENTIALS = "CREDENTIALS", GOOGLE = "GOOGLE"}
