
//import { simpleAddFunction } from "../src/index";

/*describe("simpleAddFunction", () => {

    test("adds two positive numbers", () => {
        expect(simpleAddFunction(2, 3)).toBe(5);
    });

    test("adds negative and positive numbers", () => {
        expect(simpleAddFunction(-2, 5)).toBe(3);
    });

    test("adds zero correctly", () => {
        expect(simpleAddFunction(0, 0)).toBe(0);
        expect(simpleAddFunction(0, 5)).toBe(5);
    });
});*/